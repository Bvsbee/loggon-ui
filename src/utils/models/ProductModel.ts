export default interface Product {
  id: string;
  name: string;
  species: string;
  description: string;
  categoryId: string;
  quantity: number;
  dimensions: string;
  price: number;
  image: string;
}
